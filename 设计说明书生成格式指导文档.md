# 设计说明书生成格式指导文档

> 本文档用于指导 AI 生成设计说明书。所有格式规范均以《机械臂无碰撞轨迹规划-TrajOpt算法简介.md》为范例提炼，包含：标题层级、目录生成、公式编辑、列表、强调、代码、分隔线等完整规则。

[TOC]

## 1. 总体要求

- 全文使用 **Markdown** 语法编写，以纯文本 `.md` 文件输出。
- 结构清晰、层级分明：先大纲后内容，每章每节都有明确标题。
- 公式必须使用 LaTeX 语法（KaTeX/MathJax 兼容），保证在 Typora、VS Code 等编辑器中能正常渲染。
- 内容完整：每个章节包含必要的说明、公式、步骤和示例，不省略关键推导。
- 排版整洁：段落之间空一行，章节之间可用 `---` 分隔。

## 2. 标题层级规范

标题是文档的骨架，必须按层级使用，不得跳级（如 `##` 后直接 `####`）。

### 2.1 标题层级与写法

| 级别 | 语法 | 用途 | 示例（来自范例文档） |
|------|------|------|----------------------|
| 一级标题 | `# ` | 文档总标题，仅一个 | `# 机械臂无碰撞路径规划专家对话全记录` |
| 二级标题 | `## ` | 章节，带编号 | `## 1. 咨询开始与背景询问`、`## 2. 静态障碍物七轴协作臂规划步骤` |
| 三级标题 | `### ` | 小节，带编号或步骤名 | `### 步骤一：运动学与场景建模`、`### 1. 核心思想：化曲为直` |
| 四级标题 | `#### ` | 子小节，用于公式/约束分点 | `#### 2.1 目标函数：平滑与最短`、`#### 2.2 核心约束：将“不可碰撞”凸化` |

### 2.2 标题编号规则

- **章节编号**：阿拉伯数字 + 英文点 + 空格，如 `## 1.`、`## 2.`、`## 3.`。
- **小节编号**：`步骤一 / 步骤二`（流程类）或 `1. / 2. / 3.`（原理类）。
- **子小节编号**：采用"父编号.子编号"两级制，如 `2.1`、`2.2`、`2.3`。
- 编号与标题文字之间必须有空格，格式统一为：`### 2.4 信任域约束`。
- 标题文字尽量概括本节省内容，如 `### 步骤五：执行基于采样的关节空间规划`。

### 2.3 标题示例代码

```markdown
# 文档总标题

## 1. 第一章节

### 1.1 第一小节

#### 1.1.1 第一子小节

### 1.2 第二小节

## 2. 第二章节

### 步骤一：xxx

### 步骤二：xxx
```

## 3. 目录生成方式

- 在文档总标题之后、第一章之前，单独一行写入目录占位符：

```markdown
[TOC]
```

- 该语法由 **Typora**（及其他支持 `[TOC]` 的编辑器，如 VS Code + Markdown 插件）自动识别，渲染时根据全文各级标题自动生成可点击跳转的目录树。
- 要求：
  - `[TOC]` 独占一行，前后各空一行。
  - 目录之后紧跟第一个 `##` 章节。
  - 生成说明书的 AI 只需在文档开头放置 `[TOC]`，无需手动编写目录条目。

## 4. 公式编辑方式

公式是本类说明书（含算法推导、数学建模）的核心，必须使用 **LaTeX 数学语法**，分两种形式：

### 4.1 行内公式

- 语法：`\( ... \)`
- 用途：公式嵌入在正文段落文字中，随文字流动。
- 示例：

```markdown
关节构型 \( q \in \mathbb{R}^7 \) 为一个七维列向量。
```

渲染效果：关节构型 \( q \in \mathbb{R}^7 \) 为一个七维列向量。

### 4.2 块级公式（独立公式行）

- 语法：`\[ ... \]`，公式独占一行，上下各空一行。
- 用途：重要的目标函数、约束条件、推导结果等独立展示。
- 示例：

```markdown
\[
f_{\text{smooth}} = \sum_{t=1}^{T-2} \lVert \theta_{t+2} - 2\theta_{t+1} + \theta_t \rVert^2
\]
```

渲染效果：
\[
f_{\text{smooth}} = \sum_{t=1}^{T-2} \lVert \theta_{t+2} - 2\theta_{t+1} + \theta_t \rVert^2
\]

### 4.3 常用 LaTeX 记号速查（范例文档中出现的）

| 类别 | 记号 | LaTeX 写法 |
|------|------|------------|
| 变量 | \(\theta, \psi, \delta, \varepsilon, \lambda\) | `\theta` `\psi` `\delta` `\varepsilon` `\lambda` |
| 集合 | \(\mathbb{R}^7, \mathcal{C}, \mathcal{O}_m\) | `\mathbb{R}^7` `\mathcal{C}` `\mathcal{O}_m` |
| 上下标 | \(\theta_t^{(k)}, d_{\text{safe}}, \mathbf{n}_{t,m}^{(k)\mathsf{T}}\) | `\theta_t^{(k)}` `d_{\text{safe}}` `\mathbf{n}_{t,m}^{(k)\mathsf{T}}` |
| 运算 | \(\sum_{t=1}^{T-2}, \prod, \nabla_\theta, \arg\min_i\) | `\sum_{t=1}^{T-2}` `\prod` `\nabla_\theta` `\arg\min_i` |
| 范数 | \(\lVert x \rVert^2, \| x \|_\infty\) | `\lVert x \rVert^2` `\| x \|_\infty` |
| 分数 | \(\frac{1}{2}\sum_{t=1}^{T-2}\) | `\frac{1}{2}\sum_{t=1}^{T-2}` |
| 关系 | \(\ge, \le, \in, \Rightarrow, \approx\) | `\ge` `\le` `\in` `\Rightarrow` `\approx` |
| 函数 | \(\text{sd}(\cdot), \text{ForwardKinematics}(\cdot)\) | `\text{sd}(\cdot)` `\text{ForwardKinematics}(\cdot)` |
| 矩阵/向量 | \(\mathbf{n}, \mathbf{x}, \mathsf{T}\)（转置） | `\mathbf{n}` `\mathbf{x}` `\mathsf{T}` |

### 4.4 复杂公式环境（分段函数、对齐、矩阵）

- **分段函数** `\begin{cases}`：

```markdown
\[
\mathbf{n}_{t,m}^{(k)\mathsf{T}} \Delta \theta_t \ge 
\begin{cases}
d_{\text{safe}} - d_{t,m}^{(k)}, & \text{if } d_{t,m}^{(k)} \ge 0 \\[4pt]
-d_{t,m}^{(k)} + \varepsilon, & \text{if } d_{t,m}^{(k)} < 0
\end{cases}
\]
```

- **多行对齐** `\begin{aligned}`（用于标准优化问题形式）：

```markdown
\[
\begin{aligned}
\min_{\mathbf{x}} \quad & \frac{1}{2} \mathbf{x}^\mathsf{T} P \mathbf{x} + \mathbf{q}^\mathsf{T} \mathbf{x} \\
\text{s.t.} \quad & l \le C \mathbf{x} \le u
\end{aligned}
\]
```

- **矩阵** `\begin{bmatrix}`：

```markdown
\[
\mathbf{x} = \begin{bmatrix} \Delta \theta_1 \\ \vdots \\ \Delta \theta_T \end{bmatrix}
\]
```

### 4.5 公式使用规则

- 行内公式与中文文字之间留一个空格，保证排版美观。
- 每个公式前先有一句文字说明其含义（"目标是最小化……"、"要求近似距离不小于安全阈值，即："）。
- 公式中的下标/上标必须完整：迭代上标用 `^{(k)}`、时间索引用 `_t`、障碍物索引用 `_m`。
- 重要符号首次出现时，应在正文（或"符号定义"小节）解释其含义，例如：`\( d_{\text{safe}} \) 为安全距离`。
- 公式必须保证在 Typora 中能正常渲染（KaTeX 兼容），避免使用不兼容宏包。

## 5. 对话/角色格式（问答式说明书）

范例文档是"用户-助手"对话全记录，采用加粗角色标签 + 全角冒号：

```markdown
**用户**：
提问内容……

**助手**：
回答内容……
```

规则：
- 角色标签格式固定为 `**用户**：` 和 `**助手**：`，后跟全角冒号 `：`。
- 角色标签独占一行，内容从下一行开始。
- 每个问答段落归属在对应 `##` 章节下，章节标题概括该问答主题（如 `## 3. TrajOpt（序列凸优化）详细介绍`）。
- 若文档不是对话式，而是纯说明书，则省略角色标签，直接用章节正文。

## 6. 列表格式

### 6.1 无序列表

- 使用 `- ` 开头；范例文档中列表符号后常跟 **3 个空格**再写内容（`-   `），以保证 Typora 渲染整洁。
- 列表项中先写**加粗关键词**再写说明，格式：`-   **关键词**：说明文字`。
- 示例：

```markdown
-   **碰撞模型**：将机械臂每个连杆简化为凸几何基元（如圆柱体、胶囊体）或基元组合。
-   **自碰撞模型**：定义不能相互碰撞的连杆对。
```

### 6.2 有序列表

- 使用 `1.` `2.` `3.` 编号；嵌套子步骤用字母 `a.` `b.`。
- 示例：

```markdown
1.  **初始化**：输入初始轨迹 \( \theta_{1:T}^{(0)} \)。
    a.  **碰撞检测**：对轨迹上每个点计算最近距离。
    b.  **构建 QP 子问题**：装配目标函数和约束。
```

### 6.3 嵌套列表

- 子列表相对父列表**缩进 4 个空格**。
- 示例：

```markdown
-   **分级检测**：
    1.  先用包围盒做粗检测。
    2.  对通过粗检测的基元对，执行精细的 GJK/EPA 距离计算。
    3.  若最小距离小于安全阈值 \( \epsilon \)，则判定为碰撞。
```

## 7. 强调与特殊标记

- **加粗** `**文字**`：用于关键词、术语、算法名（如 `**GJK**`、`**臂角**`、`**零空间自运动**`）。
- *斜体* `*文字*`：用于次要强调（范例中较少使用）。
- 行内代码 `` `文字` ``：用于变量名、函数名、文件路径、模块名等代码相关记号，如 `` `forward_kinematics(q)` ``、`` `robot_model.py` ``、`` `q_start` ``。
- 引用 `>`：用于文首摘要、注意事项（如本文档开头对用途的说明）。
- 删除线等其余标记：按需使用，保持克制。

## 8. 代码块

- 围栏代码块：三个反引号 ``` + 语言标识，用于展示完整代码、提示词或伪代码。
- 范例文档中在"生成代码提示词"章节使用 ```` ```markdown ```` 围栏包裹整个提示词，内部再按普通 Markdown 书写。
- 示例：

````markdown
```markdown
# 任务：七轴协作机械臂静态环境无碰撞路径规划代码实现

## 1. 环境与任务描述
- **机械臂**：七轴协作机械臂，已知标准 DH 参数或 URDF 模型。
```
````

- 规则：代码块内部不要使用 `[TOC]` 等依赖编辑器的语法；代码块前后各空一行。

## 9. 分隔线与段落

- 章节之间使用 `---` 水平分隔线，表示内容主题切换。
- 示例（范例文档中"步骤"之间、问答与正文之间的分隔）：

```markdown
### 步骤一：运动学与场景建模
……
---

### 步骤二：搭建快速碰撞检测管线
```

- 段落之间、标题与正文之间必须空一行，否则 Markdown 无法正确渲染。

## 10. 章节内容结构模板（生成说明书推荐结构）

每个章节按以下模板组织，保证完整性和可读性：

1. **章节标题**（`## 编号. 主题`）。
2. **概述段落**：1~3 句话说明本章目的/结论。
3. **小节拆分**（`### 步骤X：` 或 `### X. 主题`）：
   - 每小节先说明要点；
   - 涉及数学处给出**行内公式或块级公式**；
   - 步骤型内容用**有序/无序列表**逐条展开；
   - 关键概念用**加粗**标出。
4. **小节收尾**：给出该节小结或下一步指引（如"详见下一节"）。
5. **章节末尾**：可用 `---` 分隔，再接下一章。

### 完整示例（模仿范例文档风格）

```markdown
## 3. 核心算法原理

### 3.1 问题建模

轨迹表示为 \( T \) 个离散路径点 \( \theta_{1:T} \)，优化目标是这些路径点。

#### 3.1.1 目标函数

最小化相邻点之间的加速度平方和：

\[
f_{\text{smooth}} = \sum_{t=1}^{T-2} \lVert \theta_{t+2} - 2\theta_{t+1} + \theta_t \rVert^2
\]

#### 3.1.2 避碰约束

对每个路径点 \( \theta_t \) 和障碍物 \( \mathcal{O}_m \)，要求：

\[
\text{sd}(\theta_t, \mathcal{O}_m) \ge d_{\text{safe}}
\]

-   **符号说明**：\( \text{sd}(\cdot) \) 为符号距离函数，\( d_{\text{safe}} \) 为安全距离。
-   **注意事项**：该约束高度非凸，需在后续小节线性化处理。

---

### 3.2 求解流程

1.  **初始化**：由全局规划器生成初始轨迹。
2.  **迭代优化**：反复线性化约束并求解 QP 子问题，直至收敛。
3.  **输出**：无碰撞平滑轨迹 \( \Theta^* \)。
```

## 11. 检查清单（AI 生成后自检）

生成说明书后，逐项检查：

- [ ] 全文仅一个 `#` 一级标题，`[TOC]` 位于标题后、正文前。
- [ ] `##` 章节编号连续（1、2、3……），`###` / `####` 编号不跳级。
- [ ] 所有公式使用 `\( \)` 或 `\[ \]` 包裹，能在 Typora 中正常渲染。
- [ ] 公式前有文字说明，符号首次出现有定义。
- [ ] 列表缩进统一（子列表缩进 4 空格），无序列表 `-   ` 后跟 3 空格。
- [ ] 加粗用于关键词，行内代码用于变量/函数名，两者不混用。
- [ ] 代码块使用围栏 + 语言标识，块内不含 `[TOC]`。
- [ ] 段落、标题、代码块之间空行分隔正确。
- [ ] 章节末尾必要时用 `---` 分隔，全文格式一致。
